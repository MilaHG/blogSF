<ul class="nav navbar-nav">
    {% for category in categories %}
        <li>
            <a href="{{ path('app_category_display', {"id": category.id}) }}">
                {{ category.name }}
            </a> {#app_category_display ce sera la page d'accueil de chaque catégorie#}
        </li>
    {% endfor %}
</ul>